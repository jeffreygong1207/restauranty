# Demo Script

1. Open `/` and explain Restauranty as restaurant-controlled no-show prevention, not scalping.
2. Show `/dashboard` metrics, risk monitor, integration health, and sponsor track framing.
3. Open `/restaurants/import`, search "sushi" in "Los Angeles, CA", and import a Google Places result or seeded fallback.
4. Open `/reservations/new`, create a reservation, and show deterministic risk recalculation.
5. Open the reservation detail page and explain the risk factor breakdown.
6. Open `/recovery/res_katsuya_815` and run recovery.
7. Show waitlist ranking and fairness warnings.
8. Show World ID configured or simulated verification state on `/waitlist`.
9. Explain SMS behavior: Twilio sends only when `ENABLE_REAL_SMS=true`; otherwise notification events are simulated.
10. Approve the recovery as manager/admin and show settlement.
11. Open `/admin` to show audit logs, agent traces, and sponsor events.
12. Open `/agents` to test the local Agentverse chat endpoint.
13. Open `/integrations` to show configured/missing provider status.
